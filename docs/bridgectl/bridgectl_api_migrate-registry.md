## bridgectl api migrate-registry

Move the legacy global API registry into the selected context

### Synopsis

Copy the legacy global registry into the selected context registry, then remove the old global file. Legacy credentials must be scrubbed first and collisions require --force.

```
bridgectl api migrate-registry [flags]
```

### Options

```
      --force   Replace colliding APIs in the selected context
  -h, --help    help for migrate-registry
      --yes     Confirm removal of the legacy global registry
```

### Options inherited from parent commands

```
  -c, --context string   Override active context
  -o, --output string    Output format: table, json, yaml (default "table")
      --token string     API token for the ERPBridge server
  -v, --verbose          Show full HTTP request/response detail
```

### SEE ALSO

* [bridgectl api](bridgectl_api.md)	 - Manage ERP API endpoints

###### Auto generated by spf13/cobra on 27-Aug-2026
