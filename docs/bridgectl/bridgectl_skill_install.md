## bridgectl skill install

Install the bundled bridgectl-ops skill

```
bridgectl skill install [flags]
```

### Examples

```
  bridgectl skill install
  bridgectl skill install --project
  bridgectl skill install --dir /path/to/bridgectl-ops --force
```

### Options

```
      --dir string   Install directly into this skill directory
      --force        Replace an existing skill directory
  -h, --help         help for install
      --project      Install into ./.agents/skills in the current project
```

### Options inherited from parent commands

```
  -c, --context string   Override active context
  -o, --output string    Output format: table, json, yaml (default "table")
      --token string     API token for the ERPBridge server
  -v, --verbose          Show full HTTP request/response detail
```

### SEE ALSO

* [bridgectl skill](bridgectl_skill.md)	 - Manage bundled Agent Skills

###### Auto generated by spf13/cobra on 26-Aug-2026
