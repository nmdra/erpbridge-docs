## bridgectl web

Start the local ERPBridge Console

### Synopsis

Start a read-only local web console for monitoring configured ERPBridge contexts.

The console binds to loopback by default, keeps upstream credentials in the CLI,
and stops when this command receives an interrupt or termination signal.

```
bridgectl web [flags]
```

### Examples

```
  bridgectl web
  bridgectl web --no-open
  bridgectl web --url
```

### Options

```
      --dev             Proxy frontend development traffic to Vite on loopback
  -h, --help            help for web
      --listen string   Loopback listen address (default "127.0.0.1:0")
      --no-open         Do not open the default browser
      --url             Print the URL without opening the browser
```

### Options inherited from parent commands

```
  -c, --context string   Override active context
  -o, --output string    Output format: table, json, yaml (default "table")
      --token string     API token for the ERPBridge server
  -v, --verbose          Show full HTTP request/response detail
```

### SEE ALSO

* [bridgectl](bridgectl.md)\t - Middleware for Bridging Legacy ERP and Agentic AI

###### Auto generated by spf13/cobra on 25-Aug-2026
